import { HeroSection } from '@/components/HeroSection';

const Home = () => {
  return (
    <main className="bg-background min-h-screen">
      <HeroSection />
    </main>
  );
};

export default Home;